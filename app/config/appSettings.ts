// ═══════════════════════════════════════════════════════════════════════════
//   ⚙️  НАСТРОЙКИ ПРИЛОЖЕНИЯ  «ЗГУ 3D Модель»
//   Файл для преподавателя / администратора.
//
//   Как пользоваться:
//   • Чтобы ВКЛЮЧИТЬ функцию  — напишите  true
//   • Чтобы ВЫКЛЮЧИТЬ функцию — напишите  false
//   • Меняйте ТОЛЬКО слова true / false справа от двоеточия. Остальное не трогайте.
//   • После изменения файл нужно пересобрать (или обновить страницу в веб-версии).
// ═══════════════════════════════════════════════════════════════════════════

export const APP_SETTINGS = {
  // ─── Большие цифры над блоками здания (1…12) ───────────────────────────────
  // Это красные номера зон, парящие над корпусами. Нужны в основном для разметки.
  // По умолчанию ВЫКЛЮЧЕНО (false). Включите (true), если хотите их показать.
  showBlockNumbers: false,

  // ─── Анимации модели ───────────────────────────────────────────────────────
  // Плавное «выезжание» этажей при переключении этажей и т.п.
  // true  — анимации включены (красиво).
  // false — этажи переключаются мгновенно (чуть быстрее на очень слабом ПК).
  animations: true,

  // ─── Погода ────────────────────────────────────────────────────────────────
  // true  — погода берётся из интернета (реальная погода в Норильске).
  // false — погода НЕ берётся из интернета (по умолчанию ясно; можно менять
  //         вручную кнопками погоды внизу экрана).
  weatherByApi: true,

  // ─── Интерактивные зоны ────────────────────────────────────────────────────
  // Кликабельные зоны на здании (вестибюль, аудитории и т.д.) с фото «До/После».
  // true  — зоны показываются (по умолчанию).
  // false — зоны просто исчезают (по зданию можно гулять, но кликать не на что).
  zonesEnabled: true,

  // ─── Тени ──────────────────────────────────────────────────────────────────
  // true  — тени включены (реалистичнее).
  // false — тени выключены (заметно легче для слабых компьютеров/киосков).
  shadows: true,

  // ─── Время суток ───────────────────────────────────────────────────────────
  // true  — время суток по реальному времени (день/ночь как на улице).
  // false — всегда дневное освещение (фиксированный день).
  timeOfDayByApi: true,

  // ─── Блокировка модели ─────────────────────────────────────────────────────
  // true  — модель ЗАБЛОКИРОВАНА: на первом экране при нажатии кнопки входа
  //         появится надпись «Модель заблокирована администратором», внутрь не пустит.
  // false — модель доступна (по умолчанию).
  modelLocked: false,

  // ─── Чертежи (БТИ-планы) ───────────────────────────────────────────────────
  // Подложки-чертежи поверх этажей (нужны для разметки стен).
  // false — чертежи СКРЫТЫ (по умолчанию).
  // true  — чертежи показываются.
  blueprints: false,

  // ─── Редактор стен ─────────────────────────────────────────────────────────
  // Режим конструктора: добавление/перемещение/удаление перегородок.
  // false — редактор ВЫКЛЮЧЕН (по умолчанию) — только просмотр.
  // true  — редактор включён (появляется панель инструментов слева).
  wallEditor: false,
};

export type AppSettings = typeof APP_SETTINGS;
